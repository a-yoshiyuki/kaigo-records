<div class="container-fluid">
  <div class="row">
    <div class="col-sm-5 col-sm-offset-1">
      <h2>バイタル記録一覧</h2>
      <h4><%= Date.today %></h4>
    </div>
    <div class="col-sm-4 col-md-4"></div>
  </div>

  <div class="table-responsive">
    <table class="table">
      <div class="scroll">
        <thead>
          <tr>
            <th></th>
            <th>計測時間</th>
            <th>体温</th>
            <th>血圧</th>
            <th>心拍数</th>
            <th>SPO₂</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @customers.zip(@vital_records).each do |customers, vital_records| %>
            <tr>
              <td>
                <%= customers.full_name %>様
              </td>
                <td><%= vital_records.time.strftime('%H:%M') %></td>
                <td><%= vital_records.body_temperature %>℃</td>
                <td><%= vital_records.blood_pressure_high %>／<%= vital_records.blood_pressure_low %></td>
                <td><%= vital_records.heart_rate %>回/分</td>
                <td><%= vital_records.oxygen %>％</td>
                <td><%= link_to "編集", staff_member_vital_record_path(vital_record) %></td>
            </tr>
          <% end %>
        </tbody>
      </div>
    </div>
    <div class="col-sm-2 col-md-2"></div>
  </div>
</div>