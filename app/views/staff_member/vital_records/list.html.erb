<div class="container-fluid">
  <div class="row">
    <div class="col-sm-5 col-sm-offset-1">
      <h2>バイタル記録一覧</h2>
      <h4><%= Date.today %></h4>
    </div>
    <div class="col-sm-4 col-md-4"></div>
  </div>

  <div class="table-responsive">
    <table class="table">
      <div class="scroll">
        <thead>
          <tr>
            <th></th>
            <th>計測時間</th>
            <th>体温</th>
            <th>血圧</th>
            <th>心拍数</th>
            <th>SPO₂</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @customers.each do |customer| %>
            <tr>
              <td>
                <%= customer.full_name %>様
              </td>
              <% if customer.vital_records.present? %>
                <td><%= customer.vital_records.last.time.strftime('%H:%M') %></td>
                <td><%= customer.vital_records.last.body_temperature %>℃</td>
                <td><%= customer.vital_records.last.blood_pressure_high %>／<%= customer.vital_records.last.blood_pressure_low %></td>
                <td><%= customer.vital_records.last.heart_rate %>回/分</td>
                <td><%= customer.vital_records.last.oxygen %>％</td>
                <td><%= link_to "編集", edit_staff_member_vital_record_path(customer.vital_records.last) %></td>
              <% else %>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td><%= link_to "入力", new_staff_member_vital_record_path(customer.vital_records) %></td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </div>
    </div>
    <div class="col-sm-2 col-md-2"></div>
  </div>
</div>